name: 'dbt_project'
version: '1.0.0'
profile: 'dbt_project'

model-paths: ["dbt_project/models"]

models:
  dbt_project:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table